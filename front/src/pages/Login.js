import React from 'react';

import { UserForm } from '../components/UserForm';

export const Login = () => {
    return (
        <React.Fragment>
            <UserForm />
        </React.Fragment>
    )
}